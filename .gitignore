# dependencies (root, Frontend/, Backend/)
node_modules/

# build output
.next/
*.tsbuildinfo

# secrets — never commit real keys; .env.example (placeholders) is kept
.env
.env.local
.env.*.local
.env.development
.env.production

# tooling / local
.claude/
.DS_Store
